roverrobotics_driver:
  ros__parameters:
    diagnostics_frequency: 0.2
    odometry_frequency: 15.0
    motor_control_p_gain: 0.0011
    motor_control_i_gain: 0.000
    motor_control_d_gain: 0.00008
    publish_tf: false
    # robot_status_topic: "/"
    # robot_status_frequency: 60.0
    # robot_info_request_topic:
    # robot_info_topic:
    robot_type: "zero2"
    device_port: "/dev/rover-control"
    comm_type: "serial"
    speed_topic: "/cmd_vel"
    # estop_trigger_topic:
    # estop_reset_topic:
    # trim_topic:
    # estop_state:
    control_mode: "INDEPENDENT_WHEEL" # Closed loop : INDEPENDENT_WHEEL | Open loop : OPEN_LOOP

    # === ADD THESE TWO LINES TO OVERRIDE THE HARDCODED DEFAULTS ===
    wheel_radius: 0.008   # Put your custom measured radius here (in meters)
    wheel_base: 0.3800     # Put your custom track width here if needed
    # linear_top_speed:
    # angular_top_speed:
    odom_topic: "/odometry/wheels"
    odom_frame_id: "odom"
    odom_child_frame_id: "base_footprint" # Must match EKF base_link_frame
    # angular_a_coef:
    # angular_b_coef:
    # angular_c_coef:
    # angular_min_scale:
    # angular_max_scale:

joint_state_publisher:
  ros__parameters:
    publish_default_positions: true
    rate: 30
